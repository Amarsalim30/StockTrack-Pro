// GENERATED BY AMAR SCRIPT – EDIT MODEL
import 'package:clean_arch_app/data/models/billOfMaterialsModel.dart';
import 'package:clean_arch_app/domain/entities/billOfMaterials.dart';

class BillOfMaterialsMapper {
  static BillOfMaterials toEntity(BillOfMaterialsModel model) {
    return BillOfMaterials(
      productId: model.productId,
      components: model.components
          .map((e) => BomItemMapper.toEntity(e))
          .toList(),
      yieldPerRun: model.yieldPerRun,
      scrapPercent: model.scrapPercent,
    );
  }

  static BillOfMaterialsModel fromEntity(BillOfMaterials entity) {
    return BillOfMaterialsModel(
      productId: entity.productId,
      components: entity.components
          .map((e) => BomItemMapper.fromEntity(e))
          .toList(),
      yieldPerRun: entity.yieldPerRun,
      scrapPercent: entity.scrapPercent,
    );
  }
}
